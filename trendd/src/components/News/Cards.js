import React from 'react';

const NewsCards = ({ articles }) => {
    return (
        <div>
            {articles.map((articles, i) => (
                <NewsCards />
            ))}
        </div>
    );
}

export default NewsCards;